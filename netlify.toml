[build]
  base = "/"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps --verbose"
  CI = "false"
  NODE_OPTIONS = "--max-old-space-size=4096"
  DEBUG = "*"
